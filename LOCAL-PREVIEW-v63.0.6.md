# Techno Minds — Local Admin Preview v63.0.7

هذه نسخة محلية للمراجعة فقط. لم تُرفع إلى GitHub ولم تُنشر على Firebase أو Vercel.

## التشغيل على Windows PowerShell

```powershell
npm ci
npm test
npm run build
npm run dev
```

ثم افتح:

```text
http://localhost:5173/teacher-login.html
```

إذا كان المنفذ 5173 مستخدمًا سيعرض الأمر منفذًا بديلًا في PowerShell.

## تنبيه مهم

النسخة المحلية تستخدم إعدادات مشروع Firebase الحقيقي. تسجيل الدخول للعرض آمن، لكن الضغط على حفظ أو قبول أو حذف أو تسجيل دفع سيؤثر في بيانات المنصة الحقيقية. استخدم طالب اختبار أو بيانات اختبار فقط عند تجربة عمليات الكتابة.

## أهم التعديلات

- تنظيم القائمة إلى: اليوم، التعليم، الإدارة، النظام.
- شريط فترة العمل: العام الدراسي، الترم، الشهر، محفوظ في `sessionStorage` فقط.
- رئيسية عملية للحجوزات والتصحيح وطلبات النقل والدفع.
- إظهار عدد الطلاب المستهدفين قبل نشر المحاضرة أو الواجب أو الامتحان.
- ربط المحتوى الجديد بالسنة والترم والمسار والمجموعة و`scheduleId`.
- نموذج متجاوب لتعديل الطالب مع انتظار نجاح Firebase قبل تحديث البيانات المحلية.
- الحفاظ على سجل الدفع الشهري الحالي والدفع الجزئي والإلغاء وسجل العمليات.
- تبسيط قسم المنهج ليختص بهيكل المحاضرات والملفات وبنك الأسئلة، دون تكرار نظام التسليم والتصحيح.
- إبقاء النسخ الاحتياطية والإعدادات والتقييمات وكل الخدمات الحالية متاحة.
- نقاط تحفيز شهرية مستقلة عن الدرجات، مع إضافة أو خصم وسبب وسجل خادمي مانع للتكرار.
- ظهور نقاط التحفيز للطالب وولي الأمر دون تغيير متوسط الواجبات أو الامتحانات.
- إزالة زر «المزيد» من بوابة الطالب وإظهار الأقسام الأساسية مباشرة.
- إبقاء المحاضرات والأسئلة والاختبارات في روابط مباشرة دون تبويب محاضرات مكرر.
- حفظ كل حصة حضور بمعرّف مستقل يجمع كود الطالب وتاريخ الحصة.
- استعادة سجلات الحضور القديمة سواء استُخدم فيها `studentCode` أو `studentId` أو `code`.
- فلتر شهري للحضور؛ الشهر الحالي يبدأ بعداد صفر وتظل الشهور السابقة محفوظة.
- تحسين واجهات التحفيز والحضور لشاشات الهاتف من 320px.
- تحديث تلقائي لبوابتي الطالب وولي الأمر عند العودة للصفحة وكل 60 ثانية أثناء فتحها.
- مستمعات لحظية لتسليمات الواجبات ومحاولات الامتحانات وحركات التحفيز داخل لوحة الإدارة.
- تنبيهات داخلية للواجبات القريبة ونقاط التحفيز الجديدة.
- بحث خادمي في جميع الطلاب بالاسم أو الكود أو الهاتف، وليس في أول 200 طالب فقط.
- تحميل الطلاب على صفحات عند الطلب للحفاظ على سرعة اللوحة مع تراكم السنوات.
- ملف طالب موحد حسب العام والشهر للحضور والدرجات والواجبات والدفع والتحفيز.
- تقرير شهري قابل للنسخ، وفلتر شهري مستقل داخل تقرير ولي الأمر.
- ترتيب تحفيز شهري للمجموعة، مع تراجع آمن ينشئ حركة عكسية مسجلة ولا يحذف الأصل.
- فهارس وقواعد Firebase اللازمة، ودليل Cloud Shell مستقل للنشر والتحقق.

## نتائج التحقق داخل بيئة التجهيز

- اختبارات المصدر: 81/81 ناجحة.
- JavaScript syntax: ناجح.
- JSON وHTML والمراجع المحلية: ناجحة.
- Production build: ناجح.
- Dist verification: ناجح؛ لا Backend أو `.env` داخل `dist`.
- `npm audit --omit=dev`: صفر ثغرات.
- Functions lint وFunctions audit: ناجحان وصفر ثغرات.
- تعذر تشغيل اختبارات Emulator الجديدة داخل بيئة التجهيز لأن تنزيل Firebase CLI توقف بسبب قيد الشبكة. يجب تشغيل `npm run test:rules` في Cloud Shell قبل النشر، كما هو موضح في الدليل.

## ما لم يحدث

- لا Git push.
- لا Pull Request.
- لا Firebase deploy.
- لا Vercel deploy.
