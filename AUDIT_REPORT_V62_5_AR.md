# تقرير الفحص والإصلاح — Techno Minds V62.6.0

تم فحص النسخة المنشورة على Vercel مباشرة قبل تنفيذ هذا الإصدار، ثم ربط النتائج باختبارات المصدر والبناء.

## الإصلاحات

- استبدال اسم «أولى ثانوي برمجة» القديم في قائمة الأوائل باسم «أولى ثانوي بكالوريا».
- إضافة توافق خلفي داخل Function الترتيب حتى تظهر سجلات الطلاب القديمة والجديدة تحت المسار الموحد.
- إظهار «الكود غير صحيح أو غير موجود» في المحاضرات والأسئلة والاختبارات عند فشل التحقق من الكود.
- إضافة Canonical وOpen Graph وTwitter metadata للصفحات العامة.
- استكمال Meta Description لصفحات الأسئلة والتقييمات ودخول فريق الإدارة.
- إضافة `llms.txt` مع تعليمات صريحة بعدم طلب أو فهرسة أكواد الطلاب وبياناتهم.
- ربط Label اختيار المجموعة بعنصر Select وإضافة وصف وصول لحقل البحث والمجموعة.
- تحديث كاش PWA وإصدار الأصول إلى V62.6.0.

## ما لم يتغير

- لا يوجد حذف أو ترحيل لبيانات الطلاب.
- الاسم القديم للمسار يظل مدعومًا في البيانات الموجودة.
- لا يتم إنشاء حجوزات أو تقييمات اختبارية داخل قاعدة الإنتاج.
- إعداد App Check يحتاج تهيئة مستقلة من Firebase Console قبل فرضه على الدوال العامة، لذلك لم يُفعّل قسرًا في هذا الإصدار.

## التحقق

- يضيف الإصدار اختبارات تمنع رجوع تعارض اسم المسار ورسالة الخدمة المضللة ونقص Metadata والوصول.
- يلزم تشغيل `npm test` و`npm run build` و`npm run verify:dist` قبل النشر.
- بعد النشر يلزم تجربة كود حجز منتظر حقيقي والتأكد من ظهور «لم يتم قبول الحجز حتى الآن» مع بقاء المحتوى الأكاديمي مقفولًا حتى القبول.
