# سير العمل وسجل القرار

افحص ← أثبت المشكلة ← أصلح بأصغر تغيير متماسك ← اختبر السلوك ← وثّق الدليل والحدود.

نسخة الأساس: الملف المرفق 67.8.4، commit bf098c9. فرع المراجعة review/data-integrity. لا نشر إنتاجي، لا مراسلات، لا اختبارات حذف على بيانات حقيقية، ولا أسرار جديدة. الملفات التاريخية AUDIT_* مراجع فقط وليست إثبات تنفيذ.

قرارات هذه الدفعة:

1. حافظ على Vanilla JS/Firebase؛ استخرج الحسابات المشتركة إلى functions/lib بدل ملف patch جديد.
2. استعادة merge تحافظ على الغائب وتراجع SHA-256 والمشروع والصيغة. ارفض تعارض المال؛ لا تنفذ rollback مالي فوق دفتر حي. تحقق optimistic concurrency بين المعاينة والكتابة واحفظ نسخة أمان وسجل فشل جزئي.
3. الخزنة تقرأ الفترة الكاملة على الخادم وتعرض 40 صفًا؛ لا تعتمد إجمالياتها على أول 200 طالب أو 500 ملخص. cold reads الكاملة ما زالت مجال تحسين موثق.
4. التقرير يستبعد الدرجة المعلقة، يجمع المحاولات لكل امتحان، ويشترط إثبات اكتمال جدول الحصص؛ لا يخترع استحقاقًا ولا يسرّب student.notes.
5. الحضور يعتمد تجهيز حصة معتمدًا من الخادم ورمز attendanceCode وIndexedDB ومعرف طالب/حصة؛ الجلسة المحلية لا تمنح إذن المزامنة.
6. طلب الهيدر: العملي والنظري رابطان متجاوران، مع إصلاح CSS الذي كان يخفي النظري على سطح المكتب وتداخل الهيدر مع قائمة الهاتف.
7. رفع الإصدار إلى 67.8.5 لكسر كاش الأصول المعدلة. لا تعِد استخدام 67.8.4 للأصول المنشورة.

لتقليل إعادة العمل: ابدأ بجدول REVIEW_REPORT_AR.md وأدلة evidence ثم الملفات المتأثرة فقط. شغّل اختبار النطاق بعد كل دفعة، ومجموعة التكامل عند تغيير المعاملات/القواعد. فشل Storage الأول كان من مرور اتصال المحاكي المحلي عبر proxy بيئة الاختبار؛ نجح عند تشغيل loopback مباشرة دون تغيير الصلاحيات. أُعيد ملف اعتماد التشخيص لحالته، ولا يُشحن node_modules.

الباقي ليس «مطبقًا» لمجرد وجود هذا الملف: هاتف حقيقي وأوفلاين إعادة إطلاق PWA/الكاميرا، PDF عربي مُصدّر، مراقبة إنتاجية P95، ميزانية فعلية، تحقق IAM/الفهارس المنشورة، استرداد كائنات Storage، واستعادة مالية معزولة. لا تعلن اكتمال هذه البنود دون أدلة جديدة.

دفعة متابعة: فرع `fix/attendance-layout` من commit `6b39b3b`. إصلاح قواعد بطاقة الحضور القائمة في v55/v56 بناءً على صورة حقيقية. لا تعديلات في Functions أو القواعد أو البيانات. أضيفت بصمة SHA-256 لمحتوى CSS في روابط البناء والتجهيز المسبق للـ Service Worker؛ يتغير الرابط عند تغير CSS دون رفع إصدار الخادم أو طلب إعادة نشر جميع Functions. اختُبر هذا السلوك ببناء نسختين مختلفتين من CSS والتحقق من اختلاف الرابط وتطابق التجهيز المسبق، مع بقاء رابط JS وإصدار المنصة كما هما.

التحقق البصري الجديد متعذر في البيئة الحالية؛ لا صور «بعد» ولا قياسات أبعاد أو سرعة مُدعاة. مشكلة Cloud Run 429 منفصلة: سجلات المستخدم تثبت بدء الحاوية واجتياز TCP startup probe، وسجلات الطلب تثبت عدم وجود مثيل متاح عند الطلب؛ سبب ذلك لم يُحسم. لا تغيير في minInstances/maxInstances/IAM ولا نشر إنتاجي من هذه الجلسة.

## المتابعة الحالية — 13 سبتمبر 2026

ابدأ من `3eb73c6` (main/دمج PR18)، لا من الملف المرفق القديم. الفرع `fix/admin-theme-lesson-banks`. حافظ على إصلاح الحضور السابق وأضف PDF لكل درس في المصادر القائمة. لا تكرر تحليلات evidence السابقة على أنها نتائج جديدة.

- أثبت DOM عطل منع إعادة محاولة حفظ البنك بعد الفشل بسبب حارس النموذج ذي 900ms؛ محرر البنك يعتمد الآن قفل busy حتى انتهاء العملية.
- أثبت cascade تعارض نصوص القائمة؛ صُحح المصدر وكُبّرت النصوص الصغيرة. لا معاينة متصفح حالية بسبب تعذر الوصول المحلي ورفض مسار الملفات المحلية؛ لم تُستخدم حيلة للالتفاف على سياسة المتصفح.
- طلب المستخدم اللاحق: درجات الواجبات في بطاقة الطالب. كانت موجودة في portalResponse ولكن calcStudent المحلي استبعد homeworks. أُعيد استخدام portal-results كمصدر حساب مشترك، مع جمع الصفحات المفلترة للطلاب، وتحديث سجلات الطالب عند وصول تصحيح جديد. لا إنشاء نسخ إضافية من دفتر grades.
- الفحص العام بتاريخ 13 سبتمبر أعاد 200 لجميع 42 طلبًا للاستضافتين، بما فيها Health بإصدار 67.8.5؛ هذا لا يثبت تنفيذ الرحلات الحساسة ولا يشخص السبب التاريخي لـ429.
- لا صلاحية كتابة GitHub متاحة: محاولة push --dry-run رُفضت لغياب اعتماد HTTPS، ولم يُرفع فرع أو يُنشأ PR. التسليم كحزمة قابلة للمراجعة، دون نشر إنتاجي أو تعديل IAM/الفوترة أو إرسال رسائل.
