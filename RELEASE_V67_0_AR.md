# تسليم تطوير منصة Techno Minds — الإصدار 67.0.0

تاريخ التسليم: 6 سبتمبر 2026

## ما تم تنفيذه

### محاضرات النظري

- إلغاء الحاجة لقسم مستقل لتسجيل الحصص ودمج روابط التسجيل القديمة والجديدة داخل «محاضرات النظري».
- إضافة كل محاضرة بعنوان ووحدة ورقم ترتيب وصف ومجموعة مستهدفة.
- دعم رابط Google Drive وملف شرح اختياري داخل المحاضرة نفسها.
- ربط المحاضرة بواجب واختبار، وظهور روابطهما للطالب داخل بطاقة المحاضرة.
- إدارة حالة المحاضرة: مسودة، منشورة أو مخفية، مع أرشفة آمنة بدل الحذف المباشر.
- تعديل ونسخ ومعاينة المحاضرة، ونشر أو إخفاء مجموعة محاضرات دفعة واحدة.
- بحث وفلاتر حسب الصف والمجموعة والوحدة والحالة، مع تحميل تدريجي للقائمة.

### تجربة الطالب

- تبويب واضح للمحاضرات داخل بوابة الطالب.
- بطاقة «كمّل من حيث توقفت» في الصفحة الرئيسية للطالب.
- تجميع محاضرات النظري حسب الوحدة مع البحث وفلترة حالة التقدم.
- تسجيل فتح المحاضرة وحفظ التقدم، مع إمكانية تحديدها كمكتملة.
- دعم كامل للموبايل والوضع الداكن.

### نموذج حل الواجب

- بعد التصحيح، يظهر للطالب نموذج الحل للأسئلة التي أخطأ فيها فقط.
- كل سؤال خاطئ يعرض نص السؤال، إجابة الطالب، الإجابة الصحيحة والدرجة.
- إذا كانت كل الإجابات صحيحة تظهر رسالة نجاح بدون كشف مفاتيح إجابات إضافية.
- الواجبات الكتابية أو البرمجية تظل في انتظار تصحيح المدرس، ولا يظهر نموذج الحل قبل اعتماد الدرجة.
- مفاتيح إجابات الأسئلة الصحيحة لا ترجع أصلًا في استجابة الطالب، لتقليل تسريب الإجابات.

### الإدارة والمتابعة والأمان

- شاشة متابعة لكل محاضرة تعرض عدد المستهدفين، من فتحها، من أكملها ومن لم يفتحها.
- سجلات متابعة المحاضرات محمية من القراءة والكتابة المباشرة وتدار عن طريق Cloud Functions.
- إضافة سجلات التقدم الجديدة إلى النسخ الاحتياطية والاسترجاع بدون تغيير أو حذف البيانات القديمة.
- الإبقاء على توافق بيانات المحاضرات وروابط الحصص القديمة بدون ترحيل إجباري.

## التحقق

- اختبارات التطبيق: 125 من 125 ناجحة.
- فحص JavaScript وJSON والروابط المحلية: ناجح.
- فحص قواعد الأمان الساكنة: ناجح.
- فحص Cloud Functions: ناجح.
- بناء ملفات النشر والتحقق من عدم تسريب ملفات الخادم أو البيئة: ناجح.
- تعذر تشغيل اختبار Firebase Emulator في بيئة التجهيز فقط لأن ملفات المحاكي غير مخزنة محليًا والتنزيل الخارجي غير متاح؛ لم يفشل أي اختبار قواعد فعلي.

## النشر المطلوب لتفعيل كل المزايا

هذه الحزمة لم تُنشر تلقائيًا على الموقع الحي. لتفعيل متابعة المحاضرات ونموذج الحل والسياسات الجديدة يجب نشر الواجهة وCloud Functions وقواعد Firestore معًا بعد أخذ نسخة احتياطية:

```bash
npm test
npm run build
firebase deploy --only firestore:rules,firestore:indexes,storage,functions,hosting
```
