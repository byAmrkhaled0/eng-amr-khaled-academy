{
  "name": "technominds-platform-functions",
  "version": "70.0.5",
  "private": true,
  "main": "entry.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "lint": "node --check entry.js && node --check index.js",
    "serve": "firebase emulators:start --only functions,firestore",
    "deploy": "node -e \"console.error('Select Functions explicitly with deploy-production.ps1 -Functions names'); process.exit(1)\""
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5"
  },
  "overrides": {
    "uuid": "^11.1.1",
    "fast-xml-parser": "5.10.1",
    "qs": "6.16.0"
  },
  "description": "Firebase Cloud Functions for Techno Minds"
}
