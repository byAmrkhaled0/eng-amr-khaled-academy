# تقرير فحص منصة Techno Minds — الإصدار 62.6.0

## النتيجة

تم إصلاح سبب اختفاء الامتحانات والواجبات والمحاضرات من حساب الطالب، وتوحيد المسارات الدراسية في المنصة ولوحة الإدارة ولوحة الأوائل والتحفيز. اجتازت النسخة اختبارات المنطق والبناء وفحص روابط الصفحات والأصول محليًا.

## إصلاح ظهور محتوى الطالب

- أصبحت الامتحانات والواجبات والمحاضرات والأسئلة تستخدم مطابقة موحدة للمسار والمجموعة والترم والعام الدراسي.
- يتم جلب سجل الطالب الأساسي بالكود حتى لو كان معرّف مستند Firestore قديمًا أو عشوائيًا.
- تتم مزامنة وإصلاح نسخة `student_portal` القديمة تلقائيًا عند دخول الطالب.
- تستخدم بوابة ولي الأمر نفس سجل الطالب الأساسي حتى تعرض البيانات والمحتوى الصحيحين.
- تمنع لوحة الإدارة اختيار مجموعة من مسار مختلف عند تسجيل طالب أو إنشاء امتحان أو واجب أو محاضرة.

## المسارات المعتمدة

1. أولى ثانوي بكالوريا
2. تانية ثانوي بكالوريا
3. أساسيات برمجة
4. مبتدئين برمجة

تظل أسماء البيانات القديمة قابلة للقراءة داخليًا وتتحول إلى المسار الجديد؛ لذلك لا تضيع سجلات الطلاب أو الدرجات أو نقاط لوحة الأوائل.

## فحص المنصة

- فحص 14 صفحة HTML رئيسية وملفات JavaScript وCSS وService Worker وWeb Manifest.
- التحقق من وجود `viewport` في صفحات الطالب والاختبارات والمحتوى ولوحة الإدارة، مع نقاط توقف CSS للموبايل.
- التحقق من أن 14 صفحة و7 أصول أساسية (21 مسارًا) ترجع HTTP 200 في الخادم المحلي.
- تشغيل اختبارات Node: 26 اختبارًا ناجحًا، تشمل حماية مطابقة المحتوى وفلترة المجموعات.
- نجاح أمر البناء وفحص مجلد التوزيع لجميع الصفحات.

## النشر المطلوب

هذا الإصدار يغيّر الواجهة وFirebase Functions معًا. يجب نشر Functions وقواعد Firestore والفهارس وStorage وHosting؛ نشر Hosting وحده لن يفعّل إصلاح مطابقة الامتحانات والواجبات على الخادم. يمكن استخدام `DEPLOY-WINDOWS.cmd` أو الأمر `npm run firebase:deploy:all` بعد إعداد Firebase CLI وتسجيل الدخول.
