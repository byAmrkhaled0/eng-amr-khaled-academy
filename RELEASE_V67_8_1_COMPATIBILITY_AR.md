# إصدار 67.8.1 — إصلاح التوافق والمسارات واستجابة الأزرار

## سبب العطل المؤكد

كانت Functions المنشورة تعمل بإصدار 67.8.0، بينما ملف `firebase-sync.js` المنشور ما زال يحمل رقم واجهة 64.0.0 ويشترط تطابق رقم الإصدار حرفيًا. لذلك كانت استجابة الخادم الصحيحة تُرفض داخل المتصفح وتظهر رسالة «يوجد تحديث غير مكتمل للمنصة».

تحذير `feedback.js` وطلب `HEAD` الذي يرجع 403 صادران من أدوات Vercel المضافة للمعاينة، وليسا مسار قراءة بيانات الطالب. فحص المنصة يجب أن يستخدم `GET` للمسارات الحقيقية.

## الإصلاحات

- الاعتماد على إصدار عقد البيانات `portal-v64.0.0` بدل مساواة رقم Release بين الواجهة والخادم.
- إضافة مسارات Same-Origin للبوابة وتقرير ولي الأمر ولوحة الامتحان وبدئه وحفظه وتسليمه إلى إعدادات Vercel وFirebase Hosting.
- إبقاء Firebase Callable كمسار احتياطي عند غياب Rewrite أو حدوث عطل شبكة مؤقت.
- مهلة أقصر للمسار الأول مع عدم تكرار نفس طلب القراءة بلا داعٍ.
- منع الطلبات المتزامنة الناتجة عن الضغط المزدوج على «بدء الامتحان» وإعادة استخدام نفس Promise حتى ينتهي الطلب.
- استجابة بصرية فورية للزر، وحالة تحميل و`aria-busy`، وحارس قصير ضد إرسال النموذج مرتين.
- تحسين رسائل انتهاء المهلة حتى لا يُفسَّر بطء الاتصال على أنه انتهاء وقت الامتحان.
- فحص جميع روابط HTML والملفات والـFragments والمسارات المنشأة داخل JavaScript، وأنواع الأزرار، والنماذج المطلوبة، وتطابق API Rewrites بين الاستضافتين.

## الأداء

الزر يعطي Feedback بصريًا في أول `requestAnimationFrame`، عادة خلال أجزاء من الثانية. اكتمال عمليات Firebase يعتمد على الإنترنت وبدء Cloud Functions، ولذلك لا يمكن ضمان انتهاء كل عملية شبكية خلال ثانية، لكن الواجهة لا تبقى بلا استجابة ولا ترسل الطلب نفسه مرتين.

تمت إضافة Prefetch محدود للمسارات الداخلية عند المرور أو اللمس أو التركيز، بحد أقصى 12 مسارًا ومع احترام `Save-Data`.

## التحقق

- `npm test`: نجاح 175 من 175 اختبارًا.
- الاختبارات تغطي 16 صفحة، 107 معالجات أزرار مباشرة، مسارات Firebase/Vercel، نماذج التحقق، شاشة الامتحان والتمرير والحفظ والتسليم، الواجبات، بنوك الأسئلة، والتحفيز الشهري.
- `check-deployment.ps1` يفحص كل الصفحات الفعلية بواسطة GET، ويتأكد أن ملف المزامنة وService Worker والخادم يحملون 67.8.1، ويعرض زمن كل مسار بعد النشر.

## ترتيب النشر المطلوب

1. ارفع النسخة إلى GitHub ليبدأ نشر Vercel.
2. انشر Firebase Functions والقواعد والفهارس والتخزين.
3. بعد اكتمال Vercel شغّل:

```powershell
.\check-deployment.ps1 -BaseUrl "https://eng-amr-khaled-academy.vercel.app"
```

لا تستخدم نتيجة `HEAD` الصادرة من `feedback.js` كاختبار لصحة صفحات المنصة؛ نتيجة GET هي الفحص الصحيح.
